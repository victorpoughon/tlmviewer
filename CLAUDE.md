# Notes for Claude

## Changelog

Any breaking change to the public JavaScript API (`embed`, `load`, `loadAll`) or to the scene data format (element types, field names, matrix conventions) must be documented in `CHANGELOG.md`.
